# B5: report UNet++ + DenseCRF as the headline method.

id: exp_B5_method_unet_crf
inherits: baseline_v1

change:
  evaluation:
    focus_method: UNet_CRF

progression:
  phase: B
  step: B5
  compared_to: exp_B4_method_unet_otsu

notes: |
  Does explicit spatial coherence from DenseCRF clean up the boundary
  artifacts UNet alone produces at high SZA? The reviewer-bait row of
  Phase B.
