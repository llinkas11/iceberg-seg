# ──────────────────────────────────────────────────────────────────────────────
# Python / environments
# ──────────────────────────────────────────────────────────────────────────────
.venv/
.venv_ads/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ──────────────────────────────────────────────────────────────────────────────
# OS / editor / tool state
# ──────────────────────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
.claude/
**/.claude/
memory/
**/.ipynb_checkpoints/

# ──────────────────────────────────────────────────────────────────────────────
# Secrets (NEVER commit)
# ──────────────────────────────────────────────────────────────────────────────
.roboflow/
*.env
*.pem
*.key

# ──────────────────────────────────────────────────────────────────────────────
# Large data / artifacts that belong on HPC, not GitHub
# ──────────────────────────────────────────────────────────────────────────────

# Root-level data (audio, zips, CSVs too large or irrelevant to code)
data/

# Third-party papers - INCLUDED in archive (private repo only; do NOT make public)
# papers/

# Personal screenshots / reference images
images/

# S2-iceberg-areas generated / bulk assets
S2-iceberg-areas/chips/
S2-iceberg-areas/area_comparison/
S2-iceberg-areas/runs/
S2-iceberg-areas/figure/
S2-iceberg-areas/figures/
S2-iceberg-areas/logs/
S2-iceberg-areas/georef_predictions/
S2-iceberg-areas/predictions/
S2-iceberg-areas/sentinel2_downloads/
S2-iceberg-areas/_archive/
S2-iceberg-areas/S2UnetPlusPlus/
S2-iceberg-areas/roboflow_manual_upload_*/
# Note: ONNX, GPKG, .pth in S2-iceberg-areas/ are LFS-tracked via .gitattributes.
# Sidecar SQLite WAL files are not.
# S2-iceberg-areas/*.onnx       <- LFS-tracked
# S2-iceberg-areas/*.onnx.data  <- LFS-tracked
# S2-iceberg-areas/*.gpkg       <- LFS-tracked
S2-iceberg-areas/*.pth
S2-iceberg-areas/*.ckpt
S2-iceberg-areas/*.gpkg-shm
S2-iceberg-areas/*.gpkg-wal

# iceberg-labeler runtime state (DB + user-copied chips)
iceberg-labeler/iceberg_labels.db
iceberg-labeler/iceberg_labels.db-shm
iceberg-labeler/iceberg_labels.db-wal
iceberg-labeler/data/

# Roboflow bulk previews and example images
roboflow/roboflow_examples/
roboflow/previews/
roboflow/locator_maps/

# Paper-writing: include Overleaf source + reference PDFs in archive
paper-writing/_archive/
# paper-writing/overleaf/  <- INCLUDED for reproducibility
# paper-writing/*.pdf      <- INCLUDED (journal style guide)
paper-writing/figure_review/   # intermediate review materials, not final

# Retired figure versions (keep only the latest in fig-archive/)
paper-writing/figures/fig-archive/_archive/
paper-writing/overleaf/2026-04-17_template-plan-md/figures/fig-archive/_archive/

# ──────────────────────────────────────────────────────────────────────────────
# Large generic binary extensions (catch-all)
# ──────────────────────────────────────────────────────────────────────────────
*.tif
*.tiff
*.jp2
*.SAFE/
*.zip
*.tar
*.tar.gz
*.pkl
*.npy
*.npz
*.mp4
*.mov
*.m4a
*.wav
*.docx
*.pptx
# Allow specific archive items
!paper-writing/IDS2026_intro_methods_updated.docx

